syntax = "proto3";

package calc;
option go_package="calculator/pb";

message SumRequest {
    int64 firstInt = 1;
    int64 secondInt = 2;
}

message SumResponse {
    int64 result = 1;
}

service SumService {
    rpc Sum(SumRequest) returns (SumResponse) {};
}
